{
  "name": "n8n-video-render-service",
  "version": "1.0.0",
  "description": "Internal ffmpeg render helper for n8n (make-clip / concat). Runs as a background process inside the same container as n8n, bound to 127.0.0.1 only.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.0"
  }
}
